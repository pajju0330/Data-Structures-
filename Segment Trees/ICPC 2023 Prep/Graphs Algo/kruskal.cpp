/*
to find MST
steps::
1)sort the edges with respect to wt (wt,u,v wrt to wt)
2)greedy pickUp shortest edge
3)check if u and w belongs to same component (use disjoint set)
4)step 2 again for next edge
*/

#include <bits/stdc++.h>
#define ll long long int
using namespace std;
int main()
{   
    int n,m,u,v,wt;
    cin>>n>>m;
    return 0;
}
